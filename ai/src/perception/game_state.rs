use crate::proto::ssl_gamecontroller::Referee;
// use crate::proto::Perception;

struct TeamInfo {
    pub is_blue: bool,
    pub score: usize,
    pub goalie_id: usize,
    pub defending_positive_side: bool
}

impl TeamInfo {

}